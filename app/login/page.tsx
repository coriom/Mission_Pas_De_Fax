export default function LoginPage() {
    return (
        <main style={{ padding: 24, fontFamily: "system-ui" }}>
            <h1 style={{ fontSize: 24, fontWeight: 700 }}>
                Connexion
            </h1>
            <p style={{ marginTop: 8 }}>
                Authentification à venir (JWT / Clerk / Auth0).
            </p>
        </main>
    );
}
